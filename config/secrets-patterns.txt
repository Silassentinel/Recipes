# Recipes repo holds public recipe content pulled by ags' RecipeFetcher at
# build time to populate the live site. Nothing here is a secret, so this
# file intentionally lists no patterns -- per pre-commit-encrypt.sh, an
# empty/comments-only file means "protect nothing in this repo", overriding
# the machine-wide default-encrypt-everything behavior.
